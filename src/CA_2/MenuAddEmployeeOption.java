// This line says the enum is part of the CA_2 package
package CA_2;

// This is an enum (a list of options) called MenuAddEmployeeOption
public enum MenuAddEmployeeOption {

    // Option to add an employee manually (user types the info)
    ADD_EMPLOYEE,

    // Option to add an employee with random (automatic) data
    GENERATE_RANDOM_EMPLOYEE
}
