// This line says the enum is part of the CA_2 package
package CA_2;

// This is an enum (a list of fixed options) called MenuOption
public enum MenuOption {
    
    // Option to sort the list of employees
    SORT,

    // Option to search for an employee
    SEARCH,

    // Option to add new employee records
    ADD_RECORDS,

    // Option to exit the program
    EXIT
}
